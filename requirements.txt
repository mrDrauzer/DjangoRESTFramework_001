# Pin to a secure Django release to satisfy security audits (pip-audit/Bandit)
# Django 5.2.8 includes fixes for recent CVEs
Django>=5.2.8,<5.3
djangorestframework>=3.14,<3.16
Pillow>=10.0
drf-spectacular>=0.27,<0.28
django-filter>=24.2
psycopg2-binary>=2.9
python-dotenv>=1.0
PyJWT>=2.9
djangorestframework-simplejwt>=5.3
coverage>=7.6
pytest>=8.3
pytest-django>=4.9
pytest-html>=4.1
pytest-cov>=5.0

# Payments
stripe>=11.2

# Async tasks
celery>=5.3
redis>=5.0
django-celery-beat>=2.6

# Production server
gunicorn>=22.0

# Monitoring / Error tracking (optional)
sentry-sdk>=2.14

# Logging / Metrics (optional)
python-json-logger>=2.0
django-prometheus>=2.3

# CORS (optional)
django-cors-headers>=4.4

# Caching (optional)
django-redis>=5.4
